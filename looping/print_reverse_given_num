#Write a program to print reverse of given numbers

def function(n):
    rev=0
    while(n>0):
        rem=n%10
        rev=rev*10+(rem)
        n//=10
    return(rev)
    

n=int(input("enter the number:"))
print(function(n))
