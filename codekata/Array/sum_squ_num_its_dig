//print sum of squares of all its digits
#include <stdio.h>
int main(void)                                                                    
{
  int n,dig,sum=0,sq;
  scanf("%d",&n);
  while(n!=0)
  {
    dig=n%10;
    sq=dig*dig;
    sum=sum+sq;
    n=n/10;
  }
  printf("%d",sum);
  return 0;
}
